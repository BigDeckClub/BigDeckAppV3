{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Restart Dev Server",
      "type": "shell",
      "command": "fuser -k 5000/tcp 2>/dev/null || true && sleep 1 && npm run dev",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "problemMatcher": []
    }
  ]
}
